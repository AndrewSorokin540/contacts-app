import styled from 'styled-components';

const Wrapper = styled.div`
    padding: 10px;
    margin: 10px;
    border: 1px solid #aaa;
`;

const Name = styled.div``;

const ContactValue = styled.div`
    margin-left: 10px;
`;

export { Wrapper, Name, ContactValue };